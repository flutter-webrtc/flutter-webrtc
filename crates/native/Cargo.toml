[package]
name = "flutter-webrtc-native"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
cxx = "1.0"
libwebrtc-sys = { path = "../libwebrtc-sys" }

[build-dependencies]
anyhow = "1.0"
cxx-build = "1.0"
walkdir = "2.3"
