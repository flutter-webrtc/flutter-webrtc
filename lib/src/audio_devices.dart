class DeviceInfo {
  DeviceInfo(this.deviceId, this.label, this.groupId, this.kind);
  final String deviceId;
  final String label;
  final String groupId;
  final String kind;
}

class AudioDevice {
  
}
